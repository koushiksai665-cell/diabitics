# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# Model artifacts (regenerated by train_model.py — don't commit large binaries)
*.pkl
*.joblib

# Dataset cache (if downloaded locally instead of fetched at runtime)
*.csv
!sample_data/*.csv

# Flask
instance/
.flaskenv

# Environment variables / secrets — NEVER commit API keys
.env
.env.local
*.key

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Node (in case any tooling is added later)
node_modules/

# Logs
*.log
